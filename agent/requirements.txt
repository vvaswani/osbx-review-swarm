# Strands SDK (replaces google-adk)
strands-agents
strands-agents-tools

# Model provider (OpenRouter via LiteLLM)
litellm

# OpenSandbox (sandbox management + MCP)
opensandbox
opensandbox-mcp

# GitHub integration
PyGithub
requests

# Web framework
fastapi
uvicorn
python-dotenv
